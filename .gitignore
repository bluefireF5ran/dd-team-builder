# Dependencias y builds
node_modules/
dist/
build/
.cache/
.tmp/

# Logs y sistema
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
.DS_Store
Thumbs.db

# Entorno
.env
.env.*
!.env.example

# Imagenes (se sirven desde repo externo)
public/images

# Repositorio de assets (es un repo separado)
dd-team-builder-assets/
# Mapa old->new de la ultima pasada de scripts/nameComps.js --apply
scripts/nameComps.manifest.json

# Save de pruebas del importador (es una partida real, no del repo)
testsave/
*.lnk
